enum AdminRole {
  SUPER_ADMIN
  MODERATOR
}

enum EmploymentType {
  FULL_TIME
  PART_TIME
  CONTRACT
}

enum WorkplaceType {
  ON_SITE
  REMOTE
  HYBRID
}

enum ApplicationStatus {
  SUBMITTED
  VIEWED
  SHORTLISTED
  INTERVIEW_SCHEDULED
  REJECTED
  HIRED
}

enum FollowType {
  TEACHER
  SCHOOL
}

enum ReportStatus {
  PENDING
  RESOLVED
  DISMISSED
}

enum ContactReason {
  ORGANIZATION_SUPPORT
  EDUCATOR_SUPPORT
  TECHNICAL_ISSUE
  FEEDBACK_OR_SUGGESTION
  OTHER
}

enum LisenceStatus {
  PENDING
  APPROVED
  REJECTED
}

enum SchoolType {
  PUBLIC
  PRIVATE
}
